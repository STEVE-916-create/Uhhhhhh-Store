-- Generated by KernelPacker
--  0 dance module(s) folded, 2 other module file(s)

local modules = {}


-- Custom modules

<<<<<<< HEAD
=======
-- from: meow meow.lua
do
local ok, result = pcall(function()
local modules = {}

table.insert(modules, function()

	local m = {}
	m.ModuleType = "MOVESET"
	m.Name = "Gatito 1x1x1x1"
	m.Description = "JOIN ME- OWWWWWWWWWW!!!\n\nUpdated by Berry/Noelle1!1!!1!1!1!!1, originally by you know who. (2)"
	m.InternalName = "Gatito_1x"

	m.Assets = {
		"CatIdle.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/IwHx1HnA_CatIdle.anim",
		"CatWalk.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/U1ivDIyt_CatWalk.anim",
		"CatRun.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/ARuRri0c_CatRun.anim",
		"CatM1.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/DI2G7Q7F_CatM1.anim",
		"CatMass.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/aE3ZcepU_CatMass.anim",
		"CatKill.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/3wdw4BQs_CatKill.anim",
		"CatIntro.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/gaawgYyJ_CatIntro.anim",
		"CatShedKill.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/wrgr5dX5_CatShedKill.anim",
		"CatStunStart.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/IQVIS8sL_CatStunStart.anim",
		"CatStunLoop.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/ut6nUMFY_CatStunLoop.anim",
		"CatStunEnd.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/KDkQpJoK_CatStunEnd.anim",
		"CatVictim.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/Yu1TvOM1_CatVictim.anim",
		"CatShedVictim.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/sxzauPKO_CatShedVictim.anim",
		"CatEntanglement.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/6GZQgtFO_CatEntanglement.anim",
		"CatRotten.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/UNsamGKa_CatRotten.anim",
		"CatEye.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/YBRigu9w_CatEye.anim",

		"MinionIdle.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/Rxvba3O7_MinionIdle.anim",
		"MinionRun.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/zBLDUgfF_MinionRun.anim",
		"MinionAttack.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/UvD40d3M_MinionAttack.anim",
		"MinionSpawn.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/EKUI4U3K_MinionSpawn.anim",
		"MinionWalk.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/foJfhlcE_MinionWalk.anim",

		"CatM1.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/NO5vSaYA_CatM1.mp3",
		"Gatito.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/trtjumWX_Gatito.mp3",
		"CatKill.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/UGtNomnt_CatKill.mp3",
		"CatIntro.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/yUzF6z9J_CatIntro.mp3",
		"CatShedKill.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/b42S2Rim_CatShedKill.mp3",
		"CatStun.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/8zvrJfcu_CatStun.mp3",
		"CatMass.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/Kv7YGig4_CatMass.mp3",
		"CatEntangle.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/2FAY2xv4_CatEntangle.mp3",
		"CatRotten.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/qoMi5Ss1_CatRotten.mp3",
		"CatVoice.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/iMiPRk9r_CatVoice.mp3",
		"CatEye.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/E4eack43_CatEye.mp3",
	}

	m.Config = function(parent) end

	local Players    = game:GetService("Players")
	local Debris     = game:GetService("Debris")
	local UIS        = game:GetService("UserInputService")
	local RunService = game:GetService("RunService")
	local Player     = Players.LocalPlayer

	local animator     = nil
	local tracks       = {}
	local currentTrack = nil
	local animationTime = 0
	local lastState    = "none"
	local usingSkill   = false
	local lastIdleVoice = 0
	local isSprinting  = false

	local flingActive  = false
	local touchConns   = {}
	local figureRef    = nil
	local activeVictims = {}

	local observeConn  = nil
	local inObserveLoop = false
	local observeTarget = nil

	local voidActive   = false
	local voidHit      = false

	local activeMinions = {}
	local minionConns   = {}

	local cachedMinionAppearances = {}
	local appearancesCached       = false

	local function playSound(id, parent)
		local s = Instance.new("Sound")
		s.SoundId = AssetGetContentId(id)
		s.Volume = 1.5
		s.Parent = parent
		s:Play()
		Debris:AddItem(s, 8)
	end

	local function loadAnim(fig, file, looped)
		local a = AnimLib.Animator.new()
		a.rig    = fig
		a.track  = AnimLib.Track.fromfile(AssetGetPathFromFilename(file))
		a.looped = looped or false
		a.speed  = 1
		a.weight = 0
		return a
	end

	local function setupFlingTouched(figure)
		local function hookPart(part)
			local conn = part.Touched:Connect(function(hit)
				if not hit or not hit.Parent then return end
				local target = hit.Parent
				if target:IsA("Accessory") then target = target.Parent end
				if target == figure then return end
				if not target:FindFirstChildOfClass("Humanoid") then return end
				if voidActive then voidHit = true end
				if flingActive then ReanimateFling(target) end
			end)
			table.insert(touchConns, conn)
		end

		for _, part in ipairs(figure:GetDescendants()) do
			if part:IsA("BasePart") then hookPart(part) end
		end

		table.insert(touchConns, figure.DescendantAdded:Connect(function(part)
			if part:IsA("BasePart") then hookPart(part) end
		end))
	end

	local function getRandomPlayer()
		local valid = {}
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= Player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
				table.insert(valid, p)
			end
		end
		if #valid > 0 then return valid[math.random(1, #valid)] end
		return nil
	end

	local function spawnVictimNPC(offsetCFrame, animName, duration)
		if not figureRef then return end
		task.spawn(function()
			local targetPlayer = getRandomPlayer()
			local dummy

			if targetPlayer and targetPlayer.Character then
				pcall(function()
					targetPlayer.Character.Archivable = true
					dummy = targetPlayer.Character:Clone()
					targetPlayer.Character.Archivable = false
				end)
			end

			if not dummy then
				pcall(function()
					figureRef.Archivable = true
					dummy = figureRef:Clone()
					figureRef.Archivable = false
				end)
			end

			if not dummy then
				dummy = Players:CreateHumanoidModelFromDescription(
					Instance.new("HumanoidDescription"),
					Enum.HumanoidRigType.R6
				)
			end

			if not dummy then return end

			dummy.Name = "Meowified"
			dummy.Parent = workspace

			for _, v in ipairs(dummy:GetDescendants()) do
				if v:IsA("Script") or v:IsA("LocalScript") or v:IsA("Sound")
				or v:IsA("ScreenGui") or v:IsA("BillboardGui") or v:IsA("SurfaceGui") then
					v:Destroy()
				elseif v:IsA("BasePart") then
					v.Massless    = true
					v.CanCollide  = false
					v.CanTouch    = false
					v.CanQuery    = false
					if v.Name == "HumanoidRootPart" then
						v.Anchored = true
					else
						v.Anchored = false
					end
				end
			end

			local root   = dummy:FindFirstChild("HumanoidRootPart") or dummy:FindFirstChild("Torso")
			local myRoot = figureRef:FindFirstChild("HumanoidRootPart") or figureRef:FindFirstChild("Torso")

			if root and myRoot then
				if dummy:IsA("Model") then
					dummy.PrimaryPart = root
					dummy:PivotTo(myRoot.CFrame * offsetCFrame)
				else
					root.CFrame = myRoot.CFrame * offsetCFrame
				end
			end

			local ok, dAnim = pcall(function()
				return loadAnim(dummy, animName, false)
			end)

			if ok and dAnim then
				dAnim.weight = 1
				table.insert(activeVictims, {
					anim  = dAnim,
					dummy = dummy,
					start = os.clock()
				})
			end

			Debris:AddItem(dummy, duration + 0.3)
		end)
	end

	local function clearObserve()
		inObserveLoop = false
		observeTarget = nil
		if observeConn then
			observeConn:Disconnect()
			observeConn = nil
		end
	end

	local MINION_CHASE_RADIUS    = 30
	local MINION_CHASE_SPEED     = 8
	local MINION_WANDER_SPEED    = 4
	local MINION_ATTACK_RANGE    = 4
	local MINION_ATTACK_ANIM_DUR = 0.5
	local MINION_ATTACK_COOLDOWN = 1.75
	local MINION_WANDER_RANGE    = 20
	local MINION_WANDER_COOLDOWN = 2.0

	local MINION_SPAWN_OFFSETS = {
		CFrame.new(-4, 0, 0),
		CFrame.new( 4, 0, 0),
		CFrame.new( 0, 0, 4),
	}

	local function getClosestPlayerInRadius(minionRoot, radius)
		local bestDist   = math.huge
		local bestTarget = nil
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= Player and p.Character then
				local pRoot = p.Character:FindFirstChild("HumanoidRootPart")
				if pRoot then
					local dist = (pRoot.Position - minionRoot.Position).Magnitude
					if dist < radius and dist < bestDist then
						bestDist   = dist
						bestTarget = p
					end
				end
			end
		end
		return bestTarget, bestDist
	end

	local function minionSetAnim(minionData, animKey, looped)
		if minionData.currentAnimKey == animKey then return end
		minionData.currentAnimKey = animKey
		minionData.animator.track  = minionData.tracks[animKey]
		minionData.animator.looped = looped
		minionData.animTime        = 0
	end

	local function pickWanderTarget(minionRoot)
		local base = minionRoot.Position
		local angle = math.random() * math.pi * 2
		local dist  = math.random(5, MINION_WANDER_RANGE)
		return Vector3.new(
			base.X + math.cos(angle) * dist,
			base.Y,
			base.Z + math.sin(angle) * dist
		)
	end

	local function moveMinionToward(minionRoot, targetPos, speed, dt)
		local dir = (targetPos - minionRoot.Position)
		dir = Vector3.new(dir.X, 0, dir.Z)
		local mag = dir.Magnitude
		if mag < 0.5 then return true end
		local step = dir.Unit * speed * dt
		if step.Magnitude > mag then step = dir end
		minionRoot.CFrame = CFrame.new(minionRoot.Position + step, minionRoot.Position + dir.Unit)
		return false
	end

	local function runMinionAI(minionData, dt)
		local root = minionData.model:FindFirstChild("HumanoidRootPart")
		if not root or not minionData.model.Parent then return false end

		local now  = os.clock()
		minionData.animTime = minionData.animTime + dt
		minionData.animator:Step(minionData.animTime)

		if minionData.currentAnimKey == "attack" and not minionData.attackAnimDone then
			local elapsed = now - minionData.attackAnimStart
			if elapsed >= MINION_ATTACK_ANIM_DUR then
				minionData.attackAnimDone  = true
				minionData.onCooldown      = true
				minionData.lastAttackTime  = now
			else
				return true
			end
		end

		if minionData.onCooldown then
			minionSetAnim(minionData, "idle", true)
			if now - minionData.lastAttackTime >= MINION_ATTACK_COOLDOWN then
				minionData.onCooldown     = false
				minionData.attackAnimDone = false
				minionData.wanderTarget   = nil
				minionData.wanderArrivalTime = nil
			end
			return true
		end

		local target, dist = getClosestPlayerInRadius(root, MINION_CHASE_RADIUS)

		if target then

			local pRoot = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
			if not pRoot then return true end

			if dist <= MINION_ATTACK_RANGE then
				-- ── TRIGGER ATTACK — fling fires instantly, anim plays to completion ──
				if minionData.currentAnimKey ~= "attack" then
					minionSetAnim(minionData, "attack", false)
					minionData.attackAnimStart = now
					minionData.attackAnimDone  = false
					ReanimateFling(target.Character)
				end
			else
				-- ── RUN toward player ──
				minionSetAnim(minionData, "run", true)
				moveMinionToward(root, pRoot.Position, MINION_CHASE_SPEED, dt)
			end
		else
			-- ── WANDER MODE ──

			-- Short rest cooldown after reaching destination
			if minionData.wanderArrivalTime then
				minionSetAnim(minionData, "idle", true)
				if now - minionData.wanderArrivalTime >= MINION_WANDER_COOLDOWN then
					minionData.wanderArrivalTime = nil
					minionData.wanderTarget      = nil  -- pick new destination
				end
				return true
			end

			minionSetAnim(minionData, "walk", true)

			if not minionData.wanderTarget then
				minionData.wanderTarget = pickWanderTarget(root)
			end

			local reached = moveMinionToward(root, minionData.wanderTarget, MINION_WANDER_SPEED, dt)
			if reached then
				minionData.wanderTarget      = nil
				minionData.wanderArrivalTime = now  -- start rest timer
			end
		end

		return true
	end

	-- Fetch friend HumanoidDescriptions (online OR offline) up to `count`
	-- then pad with server players, then fallback to blank desc
	local function prefetchMinionAppearances(count)
		local appearances = {}  -- array of HumanoidDescription or nil

		-- 1. Pull friend list (works offline too, just userId-based)
		local friendIds = {}
		pcall(function()
			local pages = Players:GetFriendsAsync(Player.UserId)
			while true do
				for _, info in ipairs(pages:GetCurrentPage()) do
					table.insert(friendIds, info.Id)
					if #friendIds >= count * 3 then break end  -- grab a buffer
				end
				if pages.IsFinished or #friendIds >= count * 3 then break end
				pages:AdvanceToNextPageAsync()
			end
		end)

		-- Shuffle friend list so we don't always pick the same three
		for i = #friendIds, 2, -1 do
			local j = math.random(1, i)
			friendIds[i], friendIds[j] = friendIds[j], friendIds[i]
		end

		-- 2. Try to get HumanoidDescription for each friend (online/offline both work)
		for _, uid in ipairs(friendIds) do
			if #appearances >= count then break end
			local desc
			pcall(function()
				desc = Players:GetHumanoidDescriptionFromUserId(uid)
			end)
			if desc then
				table.insert(appearances, desc)
			end
		end

		-- 3. Pad remaining slots with server players (excluding self)
		if #appearances < count then
			for _, p in ipairs(Players:GetPlayers()) do
				if #appearances >= count then break end
				if p ~= Player then
					local desc
					pcall(function()
						desc = Players:GetHumanoidDescriptionFromUserId(p.UserId)
					end)
					if desc then
						table.insert(appearances, desc)
					end
				end
			end
		end

		-- 4. Final fallback: blank desc for any remaining slots
		while #appearances < count do
			table.insert(appearances, Instance.new("HumanoidDescription"))
		end

		return appearances
	end

	local function buildMinionModel(offsetCFrame, humanoidDesc)
		if not figureRef then return nil end

		local dummy
		-- Build from provided HumanoidDescription (friend/server player/blank)
		if humanoidDesc then
			pcall(function()
				dummy = Players:CreateHumanoidModelFromDescription(
					humanoidDesc,
					Enum.HumanoidRigType.R6
				)
			end)
		end

		-- Last resort: clone self
		if not dummy then
			pcall(function()
				figureRef.Archivable = true
				dummy = figureRef:Clone()
				figureRef.Archivable = false
			end)
		end

		if not dummy then return nil end

		dummy.Name   = "GatitoMinion"
		dummy.Parent = workspace

		-- Clean up unwanted stuff, set physics
		for _, v in ipairs(dummy:GetDescendants()) do
			if v:IsA("Script") or v:IsA("LocalScript") or v:IsA("Sound")
			or v:IsA("ScreenGui") or v:IsA("BillboardGui") or v:IsA("SurfaceGui") then
				v:Destroy()
			elseif v:IsA("BasePart") then
				v.Massless   = true
				v.CanCollide = (v.Name == "HumanoidRootPart")
				v.CanTouch   = true
				v.CanQuery   = true
				v.Anchored   = false
			end
		end

		-- Position relative to Gatito
		local myRoot = figureRef:FindFirstChild("HumanoidRootPart") or figureRef:FindFirstChild("Torso")
		local dRoot  = dummy:FindFirstChild("HumanoidRootPart") or dummy:FindFirstChild("Torso")
		if myRoot and dRoot then
			if dummy:IsA("Model") then
				dummy.PrimaryPart = dRoot
				dummy:PivotTo(myRoot.CFrame * offsetCFrame)
			else
				dRoot.CFrame = myRoot.CFrame * offsetCFrame
			end
		end

		-- Anchor root so we can drive it manually
		if dRoot then dRoot.Anchored = true end

		return dummy
	end

	local function spawnOneMinion(offsetIndex, humanoidDesc)
		if not figureRef then return end
		local offset = MINION_SPAWN_OFFSETS[offsetIndex]
		if not offset then return end

		local model = buildMinionModel(offset, humanoidDesc)
		if not model then return end

		local spawnAnim = loadAnim(model, "MinionSpawn.anim", false)
		spawnAnim.weight = 1

		local minionTracks = {
			idle   = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionIdle.anim")),
			walk   = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionWalk.anim")),
			run    = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionRun.anim")),
			attack = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionAttack.anim")),
		}

		local minionAnim  = AnimLib.Animator.new()
		minionAnim.rig    = model
		minionAnim.track  = minionTracks.idle
		minionAnim.looped = true
		minionAnim.speed  = 1
		minionAnim.weight = 1

		local minionData = {
			model             = model,
			animator          = minionAnim,
			tracks            = minionTracks,
			currentAnimKey    = "idle",
			animTime          = 0,
			wanderTarget      = nil,
			wanderArrivalTime = nil,
			onCooldown        = false,
			lastAttackTime    = 0,
			attackAnimStart   = 0,
			attackAnimDone    = false,
			spawnAnim         = spawnAnim,
			spawnStart        = os.clock(),
			spawnDone         = false,
		}

		table.insert(activeMinions, minionData)
		Debris:AddItem(model, 300)
	end

	local function destroyAllMinions()
		for _, md in ipairs(activeMinions) do
			if md.model and md.model.Parent then
				md.model:Destroy()
			end
		end
		activeMinions = {}
	end

	-- ─────────────────────────────────────────
	-- Init
	-- ─────────────────────────────────────────

	m.Init = function(figure)
		figureRef = figure
		local hum  = figure:FindFirstChildOfClass("Humanoid")
		local root = figure:FindFirstChild("HumanoidRootPart")
		if not hum or not root then return end

		animator        = AnimLib.Animator.new()
		animator.rig    = figure
		animator.looped = true

		tracks.idle         = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatIdle.anim"))
		tracks.walk         = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatWalk.anim"))
		tracks.run          = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatRun.anim"))
		tracks.m1           = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatM1.anim"))
		tracks.catMass      = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatMass.anim"))
		tracks.kill         = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatKill.anim"))
		tracks.intro        = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatIntro.anim"))
		tracks.shedKill     = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatShedKill.anim"))
		tracks.stunnedStart = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatStunStart.anim"))
		tracks.stunnedLoop  = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatStunLoop.anim"))
		tracks.stunnedEnd   = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatStunEnd.anim"))
		tracks.catEntangle  = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatEntanglement.anim"))
		tracks.catRotten    = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatRotten.anim"))
		tracks.catEye       = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatEye.anim"))

		currentTrack   = tracks.idle
		animator.track = currentTrack

		SetOverrideMovesetMusic(AssetGetContentId("Gatito.mp3"), "HATRED INCARNATE", 1)
		lastIdleVoice = os.clock()
		setupFlingTouched(figure)

		-- ── Pre-warm friend appearances asynchronously so Q-press is instant ──
		-- Runs in a fire-and-forget thread; by the time the player presses Q
		-- the network calls have already resolved and cachedMinionAppearances is full.
		task.spawn(function()
			cachedMinionAppearances = prefetchMinionAppearances(3)
			appearancesCached       = true
		end)

		-- ── M1 ──
		ContextActions:BindAction("cat_m1", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill  = true
			flingActive = true
			playSound("CatM1.mp3", root)
			currentTrack   = tracks.m1
			animator.track = currentTrack
			animationTime  = 0
			task.wait(0.90)
			flingActive   = false
			usingSkill    = false
			lastState     = "none"
		end, true, Enum.UserInputType.MouseButton1)

		-- ── MASS ──
		ContextActions:BindAction("cat_mass", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill        = true
			hum.WalkSpeed     = 0
			hum.JumpPower     = 0
			playSound("CatMass.mp3", root)
			currentTrack      = tracks.catMass
			animator.track    = currentTrack
			animationTime     = 0
			task.wait(3.08)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.E)

		-- ── KILL ──
		ContextActions:BindAction("cat_kill", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatKill.mp3", root)
			currentTrack   = tracks.kill
			animator.track = currentTrack
			animationTime  = 0
			spawnVictimNPC(CFrame.new(0,0,0) * CFrame.Angles(0, math.rad(0), 0), "CatVictim.anim", 3.05)
			task.wait(3.05)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Z)

		-- ── INTRO ──
		ContextActions:BindAction("cat_intro", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatIntro.mp3", root)
			currentTrack   = tracks.intro
			animator.track = currentTrack
			animationTime  = 0
			task.wait(3.67)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.One)

		-- ── SHED KILL ──
		ContextActions:BindAction("cat_shed_kill", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatShedKill.mp3", root)
			currentTrack   = tracks.shedKill
			animator.track = currentTrack
			animationTime  = 0
			spawnVictimNPC(CFrame.new(0,0,0) * CFrame.Angles(0, math.rad(0), 0), "CatShedVictim.anim", 4.27)
			task.wait(4.27)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Two)

		-- ── STUN ──
		ContextActions:BindAction("cat_stun", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatStun.mp3", root)
			currentTrack   = tracks.stunnedStart
			animator.track = currentTrack
			animationTime  = 0
			task.wait(0.92)
			currentTrack   = tracks.stunnedLoop
			animator.track = currentTrack
			animationTime  = 0
			task.wait(1.08)
			currentTrack   = tracks.stunnedEnd
			animator.track = currentTrack
			animationTime  = 0
			task.wait(0.92)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Three)

		-- ── ENTANGLEMENT ──
		ContextActions:BindAction("cat_entanglement", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			clearObserve()
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatEntangle.mp3", root)
			currentTrack   = tracks.catEntangle
			animator.track = currentTrack
			animationTime  = 0
			task.wait(1.67)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.R)

		-- ── CAT ROTTEN — spawns minions exactly when arms hit the ground (t=2.05s) ──
		ContextActions:BindAction("cat_rotten", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatRotten.mp3", root)
			currentTrack   = tracks.catRotten
			animator.track = currentTrack
			animationTime  = 0

			-- Despawn any alive minions from a previous cast first
			destroyAllMinions()

			-- ── USE PRE-WARMED CACHE — zero wait on keypress ──
			-- If the async pre-warm somehow hasn't finished yet (e.g. Q pressed
			-- within the first ~1-2 seconds of joining), fall back to a fresh
			-- fetch and immediately refresh the cache for the next cast.
			local minionAppearances
			if appearancesCached and #cachedMinionAppearances >= 3 then
				-- Already cooked — grab it instantly, no yield
				minionAppearances = cachedMinionAppearances

				-- Refresh the cache in the background for the NEXT cast
				-- (new random friends, any newly-joined server players, etc.)
				task.spawn(function()
					appearancesCached       = false
					cachedMinionAppearances = prefetchMinionAppearances(3)
					appearancesCached       = true
				end)
			else
				-- Rare fallback: cache wasn't ready (very early Q press)
				-- fetch synchronously — this is the old behaviour, unavoidable here
				minionAppearances = prefetchMinionAppearances(3)

				-- Also store it so the cache is warm from now on
				cachedMinionAppearances = minionAppearances
				appearancesCached       = true
			end

			-- Spawn minions across the arm slam window:
			-- Arms hit lowest point (slam) at t=2.05s
			-- Arms reach highest point after slam at t=2.25s
			-- Stagger the three spawns evenly across that 0.2s window
			task.delay(2.05, function()
				if usingSkill then spawnOneMinion(1, minionAppearances[1]) end  -- left  — at slam
			end)
			task.delay(2.15, function()
				if usingSkill then spawnOneMinion(2, minionAppearances[2]) end  -- right — mid rise
			end)
			task.delay(2.25, function()
				if usingSkill then spawnOneMinion(3, minionAppearances[3]) end  -- behind — at peak
			end)

			task.wait(3.48)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Q)

		-- ── EYE ──
		ContextActions:BindAction("cat_eye", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatEye.mp3", root)
			currentTrack   = tracks.catEye
			animator.track = currentTrack
			animationTime  = 0
			task.wait(1.75)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.T)

		-- ── SPRINT ──
		ContextActions:BindAction("cat_sprint", function(_, state)
			if state == Enum.UserInputState.Begin then
				isSprinting = not isSprinting
				ContextActions:SetTitle("cat_sprint", isSprinting and "Walk" or "Sprint")
			end
		end, true, Enum.KeyCode.LeftShift)

		-- Button titles
		ContextActions:SetTitle("cat_m1",           "M1")
		ContextActions:SetTitle("cat_mass",          "Mass")
		ContextActions:SetTitle("cat_kill",          "Kill")
		ContextActions:SetTitle("cat_intro",         "Intro")
		ContextActions:SetTitle("cat_shed_kill",     "Shed Kill")
		ContextActions:SetTitle("cat_stun",          "Stun")
		ContextActions:SetTitle("cat_entanglement",  "Entangle")
		ContextActions:SetTitle("cat_rotten",        "Rejuvenate")
		ContextActions:SetTitle("cat_eye",           "Unstable")
		ContextActions:SetTitle("cat_sprint",        "Sprint")

		-- Button positions
		ContextActions:SetPosition("cat_m1",           UDim2.new(1, -70,  1, -70))
		ContextActions:SetPosition("cat_mass",         UDim2.new(1, -140, 1, -70))
		ContextActions:SetPosition("cat_kill",         UDim2.new(1, -210, 1, -70))
		ContextActions:SetPosition("cat_intro",        UDim2.new(1, -280, 1, -70))
		ContextActions:SetPosition("cat_sprint",       UDim2.new(1, -350, 1, -70))
		ContextActions:SetPosition("cat_shed_kill",    UDim2.new(1, -70,  1, -150))
		ContextActions:SetPosition("cat_stun",         UDim2.new(1, -140, 1, -150))
		ContextActions:SetPosition("cat_entanglement", UDim2.new(1, -210, 1, -150))
		ContextActions:SetPosition("cat_rotten",       UDim2.new(1, -280, 1, -150))
		ContextActions:SetPosition("cat_eye",          UDim2.new(1, -350, 1, -150))
	end

	-- ─────────────────────────────────────────
	-- Update
	-- ─────────────────────────────────────────

	m.Update = function(dt, figure)
		if not animator then return end

		local hum  = figure:FindFirstChildOfClass("Humanoid")
		local root = figure:FindFirstChild("HumanoidRootPart")
		if not hum then return end

		animationTime += dt
		animator:Step(animationTime)

		-- Step victim anims
		local now = os.clock()
		for i = #activeVictims, 1, -1 do
			local data = activeVictims[i]
			if data.dummy and data.dummy.Parent then
				data.anim:Step(now - data.start)
			else
				table.remove(activeVictims, i)
			end
		end

		-- ── Step Minion AI ──
		for i = #activeMinions, 1, -1 do
			local md = activeMinions[i]
			if not md.model or not md.model.Parent then
				table.remove(activeMinions, i)
			else
				-- Play spawn anim first if not done
				if not md.spawnDone then
					local elapsed = now - md.spawnStart
					md.spawnAnim:Step(elapsed)
					if elapsed >= 1.6833332777023315 then
						md.spawnDone = true
					end
				else
					runMinionAI(md, dt)
				end
			end
		end

		if usingSkill then return end

		-- Idle voice random
		if os.clock() - lastIdleVoice >= 10 then
			lastIdleVoice = os.clock()
			if math.random() <= 0.4 and root then
				playSound("CatVoice.mp3", root)
			end
		end

		-- Movement state
		local state = "idle"
		if hum.MoveDirection.Magnitude > 0.1 then
			state = isSprinting and "run" or "walk"
		end

		if lastState ~= state then
			animationTime = 0
			lastState     = state
		end

		local newTrack
		if state == "idle" then
			newTrack      = tracks.idle
			hum.WalkSpeed = 8
		elseif state == "walk" then
			newTrack      = tracks.walk
			hum.WalkSpeed = 8
		elseif state == "run" then
			newTrack      = tracks.run
			hum.WalkSpeed = 27.5
		end

		if currentTrack ~= newTrack then
			currentTrack   = newTrack
			animator.track = currentTrack
		end

		animator:Step(animationTime)
	end

	-- ─────────────────────────────────────────
	-- Destroy
	-- ─────────────────────────────────────────

	m.Destroy = function()
		clearObserve()
		destroyAllMinions()

		voidActive  = false
		voidHit     = false
		isSprinting = false

		for _, conn in ipairs(touchConns) do conn:Disconnect() end
		table.clear(touchConns)

		for _, data in ipairs(activeVictims) do
			if data.dummy then data.dummy:Destroy() end
		end
		activeVictims = {}

		ContextActions:UnbindAction("cat_m1")
		ContextActions:UnbindAction("cat_mass")
		ContextActions:UnbindAction("cat_kill")
		ContextActions:UnbindAction("cat_intro")
		ContextActions:UnbindAction("cat_shed_kill")
		ContextActions:UnbindAction("cat_stun")
		ContextActions:UnbindAction("cat_entanglement")
		ContextActions:UnbindAction("cat_rotten")
		ContextActions:UnbindAction("cat_eye")
		ContextActions:UnbindAction("cat_sprint")

		animator                = nil
		tracks                  = {}
		currentTrack            = nil
		figureRef               = nil
		flingActive             = false
		usingSkill              = false
		cachedMinionAppearances = {}
		appearancesCached       = false
	end

	return m
end)

return modules

end)
if not ok then
    (warn or print)("[KernelPacker] failed to load meow meow.lua: " .. tostring(result))
elseif type(result) == "table" and result ~= modules then
    for _, fn in ipairs(result) do table.insert(modules, fn) end
end
end

>>>>>>> 3fbb5d518e4990d48f825f4f0cec92bb96aba7ca
-- from: UPDATED meow meow.lua
do
local ok, result = pcall(function()
local modules = {}
table.insert(modules, function()
	local m = {}
	m.ModuleType = "MOVESET"
	m.Name = "Gatito 1x1x1x1"
	m.Description = "JOIN ME- OWWWWWWWWWW!!!\n\nUpdated by Berry/Noelle1!1!!1!1!1!!1, originally by you know who. (1)"
	m.InternalName = "Gatito_1x"
	m.Assets = {
		"CatIdle.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/PVU0wlYv_CatIdle.anim",
		"CatWalk.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/wD7v4yqN_CatWalk.anim",
		"CatRun.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/G7kVKJxJ_CatRun.anim",
		"CatM1.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/rjXS1xFG_CatM1.anim",
		"CatMass.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/gpuCBgVV_CatMass.anim",
		"CatKill.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/ISgAhudL_CatKill.anim",
		"CatIntro.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/rnuZVSxa_CatIntro.anim",
		"CatShedKill.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/1kSQFnlL_CatShedKill.anim",
		"CatStunStart.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/DFb2MXNk_CatStunStart.anim",
		"CatStunLoop.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/FSIKaLmq_CatStunLoop.anim",
		"CatStunEnd.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/XEfImAIc_CatStunEnd.anim",
		"CatVictim.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/LoQeB1O2_CatVictim.anim",
		"CatShedVictim.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/xPU7Cwf6_CatShedVictim.anim",
		"CatEntanglement.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/eNGeoQub_CatEntanglement.anim",
		"CatRotten.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/jyHtYdvx_CatRotten.anim",
		"CatEye.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/rcAT1eJo_CatEye.anim",
		"MinionIdle.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/TujkQoeS_MinionIdle.anim",
		"MinionRun.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/1IUgndfw_MinionRun.anim",
		"MinionAttack.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/zNPnQ9J6_MinionAttack.anim",
		"MinionSpawn.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/99N0bmdS_MinionSpawn.anim",
		"MinionWalk.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/Em2iB40s_MinionWalk.anim",
		"CatM1.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/DJwOMCab_CatM1.mp3",
		"Gatito.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/dNmRMTNL_Gatito.mp3",
		"CatKill.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/SqATBDC5_CatKill.mp3",
		"CatIntro.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/Psegn2xO_CatIntro.mp3",
		"CatShedKill.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/BTsbjqam_CatShedKill.mp3",
		"CatStun.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/3l4G4FfL_CatStun.mp3",
		"CatMass.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/UFsl8SgM_CatMass.mp3",
		"CatEntangle.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/9iSm9dYy_CatEntangle.mp3",
		"CatRotten.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/VeA4uyfn_CatRotten.mp3",
		"CatVoice.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/J7RCCXdm_CatVoice.mp3",
		"CatEye.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/stxpcC1b_CatEye.mp3",
	}
	m.Config = function(parent) end
	local Players    = game:GetService("Players")
	local Debris     = game:GetService("Debris")
	local UIS        = game:GetService("UserInputService")
	local RunService = game:GetService("RunService")
	local Player     = Players.LocalPlayer
	local animator     = nil
	local tracks       = {}
	local currentTrack = nil
	local animationTime = 0
	local lastState    = "none"
	local usingSkill   = false
	local lastIdleVoice = 0
	local isSprinting  = false
	local flingActive  = false
	local touchConns   = {}
	local figureRef    = nil
	local activeVictims = {}
	local observeConn  = nil
	local inObserveLoop = false
	local observeTarget = nil
	local voidActive   = false
	local voidHit      = false
	local activeMinions = {}
	local minionConns   = {}
	local cachedMinionAppearances = {}
	local appearancesCached       = false
	local function playSound(id, parent)
		local s = Instance.new("Sound")
		s.SoundId = AssetGetContentId(id)
		s.Volume = 1.5
		s.Parent = parent
		s:Play()
		Debris:AddItem(s, 8)
	end
	local function loadAnim(fig, file, looped)
		local a = AnimLib.Animator.new()
		a.rig    = fig
		a.track  = AnimLib.Track.fromfile(AssetGetPathFromFilename(file))
		a.looped = looped or false
		a.speed  = 1
		a.weight = 0
		return a
	end
	local function setupFlingTouched(figure)
		local function hookPart(part)
			local conn = part.Touched:Connect(function(hit)
				if not hit or not hit.Parent then return end
				local target = hit.Parent
				if target:IsA("Accessory") then target = target.Parent end
				if target == figure then return end
				if not target:FindFirstChildOfClass("Humanoid") then return end
				if voidActive then voidHit = true end
				if flingActive then ReanimateFling(target) end
			end)
			table.insert(touchConns, conn)
		end
		for _, part in ipairs(figure:GetDescendants()) do
			if part:IsA("BasePart") then hookPart(part) end
		end
		table.insert(touchConns, figure.DescendantAdded:Connect(function(part)
			if part:IsA("BasePart") then hookPart(part) end
		end))
	end
	local function getRandomPlayer()
		local valid = {}
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= Player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
				table.insert(valid, p)
			end
		end
		if #valid > 0 then return valid[math.random(1, #valid)] end
		return nil
	end
	local function spawnVictimNPC(offsetCFrame, animName, duration)
		if not figureRef then return end
		task.spawn(function()
			local targetPlayer = getRandomPlayer()
			local dummy
			if targetPlayer and targetPlayer.Character then
				pcall(function()
					targetPlayer.Character.Archivable = true
					dummy = targetPlayer.Character:Clone()
					targetPlayer.Character.Archivable = false
				end)
			end
			if not dummy then
				pcall(function()
					figureRef.Archivable = true
					dummy = figureRef:Clone()
					figureRef.Archivable = false
				end)
			end
			if not dummy then
				dummy = Players:CreateHumanoidModelFromDescription(
					Instance.new("HumanoidDescription"),
					Enum.HumanoidRigType.R6
				)
			end
			if not dummy then return end
			dummy.Name = "Meowified"
			dummy.Parent = workspace
			for _, v in ipairs(dummy:GetDescendants()) do
				if v:IsA("Script") or v:IsA("LocalScript") or v:IsA("Sound")
				or v:IsA("ScreenGui") or v:IsA("BillboardGui") or v:IsA("SurfaceGui") then
					v:Destroy()
				elseif v:IsA("BasePart") then
					v.Massless    = true
					v.CanCollide  = false
					v.CanTouch    = false
					v.CanQuery    = false
					if v.Name == "HumanoidRootPart" then
						v.Anchored = true
					else
						v.Anchored = false
					end
				end
			end
			local root   = dummy:FindFirstChild("HumanoidRootPart") or dummy:FindFirstChild("Torso")
			local myRoot = figureRef:FindFirstChild("HumanoidRootPart") or figureRef:FindFirstChild("Torso")
			if root and myRoot then
				if dummy:IsA("Model") then
					dummy.PrimaryPart = root
					dummy:PivotTo(myRoot.CFrame * offsetCFrame)
				else
					root.CFrame = myRoot.CFrame * offsetCFrame
				end
			end
			local ok, dAnim = pcall(function()
				return loadAnim(dummy, animName, false)
			end)
			if ok and dAnim then
				dAnim.weight = 1
				table.insert(activeVictims, {
					anim  = dAnim,
					dummy = dummy,
					start = os.clock()
				})
			end
			Debris:AddItem(dummy, duration + 0.3)
		end)
	end
	local function clearObserve()
		inObserveLoop = false
		observeTarget = nil
		if observeConn then
			observeConn:Disconnect()
			observeConn = nil
		end
	end
	local MINION_CHASE_RADIUS    = 30
	local MINION_CHASE_SPEED     = 8
	local MINION_WANDER_SPEED    = 4
	local MINION_ATTACK_RANGE    = 4
	local MINION_ATTACK_ANIM_DUR = 0.5
	local MINION_ATTACK_COOLDOWN = 1.75
	local MINION_WANDER_RANGE    = 20
	local MINION_WANDER_COOLDOWN = 2.0
	local MINION_SPAWN_OFFSETS = {
		CFrame.new(-4, 0, 0),
		CFrame.new( 4, 0, 0),
		CFrame.new( 0, 0, 4),
	}
	local function getClosestPlayerInRadius(minionRoot, radius)
		local bestDist   = math.huge
		local bestTarget = nil
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= Player and p.Character then
				local pRoot = p.Character:FindFirstChild("HumanoidRootPart")
				if pRoot then
					local dist = (pRoot.Position - minionRoot.Position).Magnitude
					if dist < radius and dist < bestDist then
						bestDist   = dist
						bestTarget = p
					end
				end
			end
		end
		return bestTarget, bestDist
	end
	local function minionSetAnim(minionData, animKey, looped)
		if minionData.currentAnimKey == animKey then return end
		minionData.currentAnimKey = animKey
		minionData.animator.track  = minionData.tracks[animKey]
		minionData.animator.looped = looped
		minionData.animTime        = 0
	end
	local function pickWanderTarget(minionRoot)
		local base = minionRoot.Position
		local angle = math.random() * math.pi * 2
		local dist  = math.random(5, MINION_WANDER_RANGE)
		return Vector3.new(
			base.X + math.cos(angle) * dist,
			base.Y,
			base.Z + math.sin(angle) * dist
		)
	end
	local function moveMinionToward(minionRoot, targetPos, speed, dt)
		local dir = (targetPos - minionRoot.Position)
		dir = Vector3.new(dir.X, 0, dir.Z)
		local mag = dir.Magnitude
		if mag < 0.5 then return true end
		local step = dir.Unit * speed * dt
		if step.Magnitude > mag then step = dir end
		minionRoot.CFrame = CFrame.new(minionRoot.Position + step, minionRoot.Position + dir.Unit)
		return false
	end
	local function runMinionAI(minionData, dt)
		local root = minionData.model:FindFirstChild("HumanoidRootPart")
		if not root or not minionData.model.Parent then return false end
		local now  = os.clock()
		minionData.animTime = minionData.animTime + dt
		minionData.animator:Step(minionData.animTime)
		if minionData.currentAnimKey == "attack" and not minionData.attackAnimDone then
			local elapsed = now - minionData.attackAnimStart
			if elapsed >= MINION_ATTACK_ANIM_DUR then
				minionData.attackAnimDone  = true
				minionData.onCooldown      = true
				minionData.lastAttackTime  = now
			else
				return true
			end
		end
		if minionData.onCooldown then
			minionSetAnim(minionData, "idle", true)
			if now - minionData.lastAttackTime >= MINION_ATTACK_COOLDOWN then
				minionData.onCooldown     = false
				minionData.attackAnimDone = false
				minionData.wanderTarget   = nil
				minionData.wanderArrivalTime = nil
			end
			return true
		end
		local target, dist = getClosestPlayerInRadius(root, MINION_CHASE_RADIUS)
		if target then
			local pRoot = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
			if not pRoot then return true end
			if dist <= MINION_ATTACK_RANGE then
				if minionData.currentAnimKey ~= "attack" then
					minionSetAnim(minionData, "attack", false)
					minionData.attackAnimStart = now
					minionData.attackAnimDone  = false
					ReanimateFling(target.Character)
				end
			else
				minionSetAnim(minionData, "run", true)
				moveMinionToward(root, pRoot.Position, MINION_CHASE_SPEED, dt)
			end
		else
			if minionData.wanderArrivalTime then
				minionSetAnim(minionData, "idle", true)
				if now - minionData.wanderArrivalTime >= MINION_WANDER_COOLDOWN then
					minionData.wanderArrivalTime = nil
					minionData.wanderTarget      = nil  
				end
				return true
			end
			minionSetAnim(minionData, "walk", true)
			if not minionData.wanderTarget then
				minionData.wanderTarget = pickWanderTarget(root)
			end
			local reached = moveMinionToward(root, minionData.wanderTarget, MINION_WANDER_SPEED, dt)
			if reached then
				minionData.wanderTarget      = nil
				minionData.wanderArrivalTime = now  
			end
		end
		return true
	end
	local function prefetchMinionAppearances(count)
		local appearances = {}  
		local friendIds = {}
		pcall(function()
			local pages = Players:GetFriendsAsync(Player.UserId)
			while true do
				for _, info in ipairs(pages:GetCurrentPage()) do
					table.insert(friendIds, info.Id)
					if #friendIds >= count * 3 then break end  
				end
				if pages.IsFinished or #friendIds >= count * 3 then break end
				pages:AdvanceToNextPageAsync()
			end
		end)
		for i = #friendIds, 2, -1 do
			local j = math.random(1, i)
			friendIds[i], friendIds[j] = friendIds[j], friendIds[i]
		end
		for _, uid in ipairs(friendIds) do
			if #appearances >= count then break end
			local desc
			pcall(function()
				desc = Players:GetHumanoidDescriptionFromUserId(uid)
			end)
			if desc then
				table.insert(appearances, desc)
			end
		end
		if #appearances < count then
			for _, p in ipairs(Players:GetPlayers()) do
				if #appearances >= count then break end
				if p ~= Player then
					local desc
					pcall(function()
						desc = Players:GetHumanoidDescriptionFromUserId(p.UserId)
					end)
					if desc then
						table.insert(appearances, desc)
					end
				end
			end
		end
		while #appearances < count do
			table.insert(appearances, Instance.new("HumanoidDescription"))
		end
		return appearances
	end
	local function buildMinionModel(offsetCFrame, humanoidDesc)
		if not figureRef then return nil end
		local dummy
		if humanoidDesc then
			pcall(function()
				dummy = Players:CreateHumanoidModelFromDescription(
					humanoidDesc,
					Enum.HumanoidRigType.R6
				)
			end)
		end
		if not dummy then
			pcall(function()
				figureRef.Archivable = true
				dummy = figureRef:Clone()
				figureRef.Archivable = false
			end)
		end
		if not dummy then return nil end
		dummy.Name   = "GatitoMinion"
		dummy.Parent = workspace
		for _, v in ipairs(dummy:GetDescendants()) do
			if v:IsA("Script") or v:IsA("LocalScript") or v:IsA("Sound")
			or v:IsA("ScreenGui") or v:IsA("BillboardGui") or v:IsA("SurfaceGui") then
				v:Destroy()
			elseif v:IsA("BasePart") then
				v.Massless   = true
				v.CanCollide = (v.Name == "HumanoidRootPart")
				v.CanTouch   = true
				v.CanQuery   = true
				v.Anchored   = false
			end
		end
		local myRoot = figureRef:FindFirstChild("HumanoidRootPart") or figureRef:FindFirstChild("Torso")
		local dRoot  = dummy:FindFirstChild("HumanoidRootPart") or dummy:FindFirstChild("Torso")
		if myRoot and dRoot then
			if dummy:IsA("Model") then
				dummy.PrimaryPart = dRoot
				dummy:PivotTo(myRoot.CFrame * offsetCFrame)
			else
				dRoot.CFrame = myRoot.CFrame * offsetCFrame
			end
		end
		if dRoot then dRoot.Anchored = true end
		return dummy
	end
	local function spawnOneMinion(offsetIndex, humanoidDesc)
		if not figureRef then return end
		local offset = MINION_SPAWN_OFFSETS[offsetIndex]
		if not offset then return end
		local model = buildMinionModel(offset, humanoidDesc)
		if not model then return end
		local spawnAnim = loadAnim(model, "MinionSpawn.anim", false)
		spawnAnim.weight = 1
		local minionTracks = {
			idle   = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionIdle.anim")),
			walk   = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionWalk.anim")),
			run    = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionRun.anim")),
			attack = AnimLib.Track.fromfile(AssetGetPathFromFilename("MinionAttack.anim")),
		}
		local minionAnim  = AnimLib.Animator.new()
		minionAnim.rig    = model
		minionAnim.track  = minionTracks.idle
		minionAnim.looped = true
		minionAnim.speed  = 1
		minionAnim.weight = 1
		local minionData = {
			model             = model,
			animator          = minionAnim,
			tracks            = minionTracks,
			currentAnimKey    = "idle",
			animTime          = 0,
			wanderTarget      = nil,
			wanderArrivalTime = nil,
			onCooldown        = false,
			lastAttackTime    = 0,
			attackAnimStart   = 0,
			attackAnimDone    = false,
			spawnAnim         = spawnAnim,
			spawnStart        = os.clock(),
			spawnDone         = false,
		}
		table.insert(activeMinions, minionData)
		Debris:AddItem(model, 300)
	end
	local function destroyAllMinions()
		for _, md in ipairs(activeMinions) do
			if md.model and md.model.Parent then
				md.model:Destroy()
			end
		end
		activeMinions = {}
	end
	m.Init = function(figure)
		figureRef = figure
		local hum  = figure:FindFirstChildOfClass("Humanoid")
		local root = figure:FindFirstChild("HumanoidRootPart")
		if not hum or not root then return end
		animator        = AnimLib.Animator.new()
		animator.rig    = figure
		animator.looped = true
		tracks.idle         = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatIdle.anim"))
		tracks.walk         = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatWalk.anim"))
		tracks.run          = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatRun.anim"))
		tracks.m1           = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatM1.anim"))
		tracks.catMass      = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatMass.anim"))
		tracks.kill         = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatKill.anim"))
		tracks.intro        = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatIntro.anim"))
		tracks.shedKill     = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatShedKill.anim"))
		tracks.stunnedStart = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatStunStart.anim"))
		tracks.stunnedLoop  = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatStunLoop.anim"))
		tracks.stunnedEnd   = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatStunEnd.anim"))
		tracks.catEntangle  = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatEntanglement.anim"))
		tracks.catRotten    = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatRotten.anim"))
		tracks.catEye       = AnimLib.Track.fromfile(AssetGetPathFromFilename("CatEye.anim"))
		currentTrack   = tracks.idle
		animator.track = currentTrack
		SetOverrideMovesetMusic(AssetGetContentId("Gatito.mp3"), "HATRED INCARNATE", 1)
		lastIdleVoice = os.clock()
		setupFlingTouched(figure)
		task.spawn(function()
			cachedMinionAppearances = prefetchMinionAppearances(3)
			appearancesCached       = true
		end)
		ContextActions:BindAction("cat_m1", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill  = true
			flingActive = true
			playSound("CatM1.mp3", root)
			currentTrack   = tracks.m1
			animator.track = currentTrack
			animationTime  = 0
			task.wait(0.90)
			flingActive   = false
			usingSkill    = false
			lastState     = "none"
		end, true, Enum.UserInputType.MouseButton1)
		ContextActions:BindAction("cat_mass", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill        = true
			hum.WalkSpeed     = 0
			hum.JumpPower     = 0
			playSound("CatMass.mp3", root)
			currentTrack      = tracks.catMass
			animator.track    = currentTrack
			animationTime     = 0
			task.wait(3.08)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.E)
		ContextActions:BindAction("cat_kill", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatKill.mp3", root)
			currentTrack   = tracks.kill
			animator.track = currentTrack
			animationTime  = 0
			spawnVictimNPC(CFrame.new(0,0,0) * CFrame.Angles(0, math.rad(0), 0), "CatVictim.anim", 3.05)
			task.wait(3.05)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Z)
		ContextActions:BindAction("cat_intro", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatIntro.mp3", root)
			currentTrack   = tracks.intro
			animator.track = currentTrack
			animationTime  = 0
			task.wait(3.67)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.One)
		ContextActions:BindAction("cat_shed_kill", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatShedKill.mp3", root)
			currentTrack   = tracks.shedKill
			animator.track = currentTrack
			animationTime  = 0
			spawnVictimNPC(CFrame.new(0,0,0) * CFrame.Angles(0, math.rad(0), 0), "CatShedVictim.anim", 4.27)
			task.wait(4.27)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Two)
		ContextActions:BindAction("cat_stun", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatStun.mp3", root)
			currentTrack   = tracks.stunnedStart
			animator.track = currentTrack
			animationTime  = 0
			task.wait(0.92)
			currentTrack   = tracks.stunnedLoop
			animator.track = currentTrack
			animationTime  = 0
			task.wait(1.08)
			currentTrack   = tracks.stunnedEnd
			animator.track = currentTrack
			animationTime  = 0
			task.wait(0.92)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Three)
		ContextActions:BindAction("cat_entanglement", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			clearObserve()
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatEntangle.mp3", root)
			currentTrack   = tracks.catEntangle
			animator.track = currentTrack
			animationTime  = 0
			task.wait(1.67)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.R)
		ContextActions:BindAction("cat_rotten", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatRotten.mp3", root)
			currentTrack   = tracks.catRotten
			animator.track = currentTrack
			animationTime  = 0
			destroyAllMinions()
			local minionAppearances
			if appearancesCached and #cachedMinionAppearances >= 3 then
				minionAppearances = cachedMinionAppearances
				task.spawn(function()
					appearancesCached       = false
					cachedMinionAppearances = prefetchMinionAppearances(3)
					appearancesCached       = true
				end)
			else
				minionAppearances = prefetchMinionAppearances(3)
				cachedMinionAppearances = minionAppearances
				appearancesCached       = true
			end
			task.delay(2.05, function()
				if usingSkill then spawnOneMinion(1, minionAppearances[1]) end  
			end)
			task.delay(2.15, function()
				if usingSkill then spawnOneMinion(2, minionAppearances[2]) end  
			end)
			task.delay(2.25, function()
				if usingSkill then spawnOneMinion(3, minionAppearances[3]) end  
			end)
			task.wait(3.48)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.Q)
		ContextActions:BindAction("cat_eye", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill     = true
			hum.WalkSpeed  = 0
			hum.JumpPower  = 0
			playSound("CatEye.mp3", root)
			currentTrack   = tracks.catEye
			animator.track = currentTrack
			animationTime  = 0
			task.wait(1.75)
			usingSkill    = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState     = "none"
		end, true, Enum.KeyCode.T)
		ContextActions:BindAction("cat_sprint", function(_, state)
			if state == Enum.UserInputState.Begin then
				isSprinting = not isSprinting
				ContextActions:SetTitle("cat_sprint", isSprinting and "Walk" or "Sprint")
			end
		end, true, Enum.KeyCode.LeftShift)
		ContextActions:SetTitle("cat_m1",           "M1")
		ContextActions:SetTitle("cat_mass",          "Mass")
		ContextActions:SetTitle("cat_kill",          "Kill")
		ContextActions:SetTitle("cat_intro",         "Intro")
		ContextActions:SetTitle("cat_shed_kill",     "Shed Kill")
		ContextActions:SetTitle("cat_stun",          "Stun")
		ContextActions:SetTitle("cat_entanglement",  "Entangle")
		ContextActions:SetTitle("cat_rotten",        "Rejuvenate")
		ContextActions:SetTitle("cat_eye",           "Unstable")
		ContextActions:SetTitle("cat_sprint",        "Sprint")
		ContextActions:SetPosition("cat_m1",           UDim2.new(1, -70,  1, -70))
		ContextActions:SetPosition("cat_mass",         UDim2.new(1, -140, 1, -70))
		ContextActions:SetPosition("cat_kill",         UDim2.new(1, -210, 1, -70))
		ContextActions:SetPosition("cat_intro",        UDim2.new(1, -280, 1, -70))
		ContextActions:SetPosition("cat_sprint",       UDim2.new(1, -350, 1, -70))
		ContextActions:SetPosition("cat_shed_kill",    UDim2.new(1, -70,  1, -150))
		ContextActions:SetPosition("cat_stun",         UDim2.new(1, -140, 1, -150))
		ContextActions:SetPosition("cat_entanglement", UDim2.new(1, -210, 1, -150))
		ContextActions:SetPosition("cat_rotten",       UDim2.new(1, -280, 1, -150))
		ContextActions:SetPosition("cat_eye",          UDim2.new(1, -350, 1, -150))
	end
	m.Update = function(dt, figure)
		if not animator then return end
		local hum  = figure:FindFirstChildOfClass("Humanoid")
		local root = figure:FindFirstChild("HumanoidRootPart")
		if not hum then return end
		animationTime += dt
		animator:Step(animationTime)
		local now = os.clock()
		for i = #activeVictims, 1, -1 do
			local data = activeVictims[i]
			if data.dummy and data.dummy.Parent then
				data.anim:Step(now - data.start)
			else
				table.remove(activeVictims, i)
			end
		end
		for i = #activeMinions, 1, -1 do
			local md = activeMinions[i]
			if not md.model or not md.model.Parent then
				table.remove(activeMinions, i)
			else
				if not md.spawnDone then
					local elapsed = now - md.spawnStart
					md.spawnAnim:Step(elapsed)
					if elapsed >= 1.6833332777023315 then
						md.spawnDone = true
					end
				else
					runMinionAI(md, dt)
				end
			end
		end
		if usingSkill then return end
		if os.clock() - lastIdleVoice >= 10 then
			lastIdleVoice = os.clock()
			if math.random() <= 0.4 and root then
				playSound("CatVoice.mp3", root)
			end
		end
		local state = "idle"
		if hum.MoveDirection.Magnitude > 0.1 then
			state = isSprinting and "run" or "walk"
		end
		if lastState ~= state then
			animationTime = 0
			lastState     = state
		end
		local newTrack
		if state == "idle" then
			newTrack      = tracks.idle
			hum.WalkSpeed = 8
		elseif state == "walk" then
			newTrack      = tracks.walk
			hum.WalkSpeed = 8
		elseif state == "run" then
			newTrack      = tracks.run
			hum.WalkSpeed = 27.5
		end
		if currentTrack ~= newTrack then
			currentTrack   = newTrack
			animator.track = currentTrack
		end
		animator:Step(animationTime)
	end
	m.Destroy = function()
		clearObserve()
		destroyAllMinions()
		voidActive  = false
		voidHit     = false
		isSprinting = false
		for _, conn in ipairs(touchConns) do conn:Disconnect() end
		table.clear(touchConns)
		for _, data in ipairs(activeVictims) do
			if data.dummy then data.dummy:Destroy() end
		end
		activeVictims = {}
		ContextActions:UnbindAction("cat_m1")
		ContextActions:UnbindAction("cat_mass")
		ContextActions:UnbindAction("cat_kill")
		ContextActions:UnbindAction("cat_intro")
		ContextActions:UnbindAction("cat_shed_kill")
		ContextActions:UnbindAction("cat_stun")
		ContextActions:UnbindAction("cat_entanglement")
		ContextActions:UnbindAction("cat_rotten")
		ContextActions:UnbindAction("cat_eye")
		ContextActions:UnbindAction("cat_sprint")
		animator                = nil
		tracks                  = {}
		currentTrack            = nil
		figureRef               = nil
		flingActive             = false
		usingSkill              = false
		cachedMinionAppearances = {}
		appearancesCached       = false
	end
	return m
end)
return modules

end)
if not ok then
    (warn or print)("[KernelPacker] failed to load UPDATED meow meow.lua: " .. tostring(result))
elseif type(result) == "table" and result ~= modules then
    for _, fn in ipairs(result) do table.insert(modules, fn) end
end
end

-- from: UPDATED mind of him.lua
do
local ok, result = pcall(function()
local modules = {}
table.insert(modules, function()
	local m = {}
	m.ModuleType = "MOVESET"
	m.Name = "ph0nk1dd"
	m.Description = "the mind of him.\n\nUpd4t3d by b3rry/n03ll3!1!!1!1, 0r1g1n4lly by.. d0 1 h4v3 t0 s4y th1s 4g4in?...."
	m.InternalName = "filth_remnant"
	m.Assets = {
		"PHONKIDLE.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/IHlHL76w_PHONKIDLE.anim",
		"PHONKWALK.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/buqtStGq_PHONKWALK.anim",
		"PHONKRUN.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/qmST9T5p_PHONKRUN.anim",
		"PHONKM1.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/crEcRvdp_PHONKM1.anim",
		"PHONKCORRUPT.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/3mVCjRYk_PHONKCORRUPT.anim",
		"PHONKKILL.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/9cDRw87M_PHONKKILL.anim",
		"PHONKINTRO.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/kNvhwpuz_PHONKINTRO.anim",
		"PHONK7N7KILL.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/EZBjiDw9_PHONK7N7KILL.anim",
		"PHONKSTUNSTART.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/PhBZWp7V_PHONKSTUNSTART.anim",
		"PHONKSTUNLOOP.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/qzDMxymF_PHONKSTUNLOOP.anim",
		"PHONKSTUNEND.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/cGKyT4Vn_PHONKSTUNEND.anim",
		"PHONKVICTIM.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/Rlu0MQZr_PHONKVICTIM.anim",
		"PHONK7N7VICTIM.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/YpeIEcuS_PHONK7N7VICTIM.anim",
		"PHONKDELIVERY.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/BVsWhFpj_PHONKDELIVERY.anim",
		"PHONKWALKSPEEDSTART.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/bXGURp5z_PHONKWALKSPEEDSTART.anim",
		"PHONKWALKSPEEDLOOP.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/ynna3aqP_PHONKWALKSPEEDLOOP.anim",
		"PHONKWALKSPEEDHIT.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/puoSuIAT_PHONKWALKSPEEDHIT.anim",
		"PHONKWALKSPEEDFAIL.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/rTDymSGl_PHONKWALKSPEEDFAIL.anim",
		"PhonkMinionSpawnLeft.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/Da3lnYIM_PhonkMinionSpawnLeft.anim",
		"PhonkMinionSpawnRight.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/9TK2T0I7_PhonkMinionSpawnRight.anim",
		"PhonkMinionIdle.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/jUPDsdnH_PhonkMinionIdle.anim",
		"PhonkMinionAttack.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/WO36vzhY_PhonkMinionAttack.anim",
		"PhonkMinionWalk.anim@MARKET/MARKET_GatitoandPh0nk1dd/assets/za9NWFnV_PhonkMinionWalk.anim",
		"PHONKM1.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/mXO6Ok3X_PHONKM1.mp3",
		"PHONKCORRUPT.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/4GokJzRu_PHONKCORRUPT.mp3",
		"PHONKDELIVERYVOICE.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/eIqz8ynv_PHONKDELIVERYVOICE.mp3",
		"PHONKKILL.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/wl6jOj41_PHONKKILL.mp3",
		"PHONKINTRO.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/5GVHQMsI_PHONKINTRO.mp3",
		"PHONK7N7KILL.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/iGSR6x98_PHONK7N7KILL.mp3",
		"PHONK7N7KILLEFFECT.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/6qv2MGl8_PHONK7N7KILLEFFECT.mp3",
		"PHONKSTUN.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/Rifupajk_PHONKSTUN.mp3",
		"PHONKWALKSPEEDSTART.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/E4PjG7RO_PHONKWALKSPEEDSTART.mp3",
		"PHONKWALKSPEEDLOOP.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/EytyTtwN_PHONKWALKSPEEDLOOP.mp3",
		"PHONKWALKSPEEDHIT.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/HPFhlRd1_PHONKWALKSPEEDHIT.mp3",
		"PHONKWALKSPEEDFAIL.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/XjlchulL_PHONKWALKSPEEDFAIL.mp3",
		"PHONKDELIVERY.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/o0USYAuq_PHONKDELIVERY.mp3",
		"Phonk.mp3@MARKET/MARKET_GatitoandPh0nk1dd/assets/WNOF48jz_Phonk.mp3",
	}
	m.Config = function(parent) end
	local Players = game:GetService("Players")
	local Debris = game:GetService("Debris")
	local UIS = game:GetService("UserInputService")
	local Player = Players.LocalPlayer
	local animator = nil
	local tracks = {}
	local currentTrack = nil
	local animationTime = 0
	local lastState = "none"
	local usingSkill = false
	local isSprinting = false
	local flingActive = false
	local touchConns = {}
	local figureRef = nil
	local activeVictims = {}
	local observeConn = nil
	local inObserveLoop = false
	local observeTarget = nil
	local voidActive = false
	local voidHit = false
	local activeMinions = {}
	local cachedMinionAppearances = {}
	local appearancesCached = false
	local function playSound(id, parent)
		local s = Instance.new("Sound")
		s.SoundId = AssetGetContentId(id)
		s.Volume = 1.5
		s.Parent = parent
		s:Play()
		Debris:AddItem(s, 8)
	end
	local function loadAnim(fig, file, looped)
		local a = AnimLib.Animator.new()
		a.rig = fig
		a.track = AnimLib.Track.fromfile(AssetGetPathFromFilename(file))
		a.looped = looped or false
		a.speed = 1
		a.weight = 0
		return a
	end
	local function setupFlingTouched(figure)
		local function hookPart(part)
			local conn = part.Touched:Connect(function(hit)
				if not hit or not hit.Parent then return end
				local target = hit.Parent
				if target:IsA("Accessory") then target = target.Parent end
				if target == figure then return end
				if not target:FindFirstChildOfClass("Humanoid") then return end
				if voidActive then
					voidHit = true
				end
				if flingActive then
					ReanimateFling(target)
				end
			end)
			table.insert(touchConns, conn)
		end
		for _, part in ipairs(figure:GetDescendants()) do
			if part:IsA("BasePart") then
				hookPart(part)
			end
		end
		table.insert(touchConns, figure.DescendantAdded:Connect(function(part)
			if part:IsA("BasePart") then
				hookPart(part)
			end
		end))
	end
	local function getRandomPlayer()
		local valid = {}
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= Player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
				table.insert(valid, p)
			end
		end
		if #valid > 0 then
			return valid[math.random(1, #valid)]
		end
		return nil
	end
	local function spawnVictimNPC(offsetCFrame, animName, duration)
		if not figureRef then return end
		task.spawn(function()
			local targetPlayer = getRandomPlayer()
			local dummy
			if targetPlayer and targetPlayer.Character then
				pcall(function()
					targetPlayer.Character.Archivable = true
					dummy = targetPlayer.Character:Clone()
					targetPlayer.Character.Archivable = false
				end)
			end
			if not dummy then
				pcall(function()
					figureRef.Archivable = true
					dummy = figureRef:Clone()
					figureRef.Archivable = false
				end)
			end
			if not dummy then
				dummy = Players:CreateHumanoidModelFromDescription(
					Instance.new("HumanoidDescription"),
					Enum.HumanoidRigType.R6
				)
			end
			if not dummy then return end
			dummy.Name = "Phonked"
			dummy.Parent = workspace
			for _, v in ipairs(dummy:GetDescendants()) do
				if v:IsA("Script") or v:IsA("LocalScript") or v:IsA("Sound")
				or v:IsA("ScreenGui") or v:IsA("BillboardGui") or v:IsA("SurfaceGui") then
					v:Destroy()
				elseif v:IsA("BasePart") then
					v.Massless = true
					v.CanCollide = false
					v.CanTouch = false
					v.CanQuery = false
					if v.Name == "HumanoidRootPart" then
						v.Anchored = true
					else
						v.Anchored = false
					end
				end
			end
			local root = dummy:FindFirstChild("HumanoidRootPart") or dummy:FindFirstChild("Torso")
			local myRoot = figureRef:FindFirstChild("HumanoidRootPart") or figureRef:FindFirstChild("Torso")
			if root and myRoot then
				if dummy:IsA("Model") then
					dummy.PrimaryPart = root
					dummy:PivotTo(myRoot.CFrame * offsetCFrame)
				else
					root.CFrame = myRoot.CFrame * offsetCFrame
				end
			end
			local ok, dAnim = pcall(function()
				return loadAnim(dummy, animName, false)
			end)
			if ok and dAnim then
				dAnim.weight = 1
				table.insert(activeVictims, {
					anim = dAnim,
					dummy = dummy,
					start = os.clock()
				})
			end
			Debris:AddItem(dummy, duration + 0.3)
		end)
	end
	local function clearObserve()
		inObserveLoop = false
		observeTarget = nil
		if observeConn then
			observeConn:Disconnect()
			observeConn = nil
		end
	end
	local function prefetchMinionAppearances(count)
		local appearances = {}
		local friendIds = {}
		pcall(function()
			local pages = Players:GetFriendsAsync(Player.UserId)
			while true do
				for _, info in ipairs(pages:GetCurrentPage()) do
					table.insert(friendIds, info.Id)
					if #friendIds >= count * 3 then break end
				end
				if pages.IsFinished or #friendIds >= count * 3 then break end
				pages:AdvanceToNextPageAsync()
			end
		end)
		for i = #friendIds, 2, -1 do
			local j = math.random(1, i)
			friendIds[i], friendIds[j] = friendIds[j], friendIds[i]
		end
		for _, uid in ipairs(friendIds) do
			if #appearances >= count then break end
			local desc
			pcall(function()
				desc = Players:GetHumanoidDescriptionFromUserId(uid)
			end)
			if desc then
				table.insert(appearances, desc)
			end
		end
		if #appearances < count then
			for _, p in ipairs(Players:GetPlayers()) do
				if #appearances >= count then break end
				if p ~= Player then
					local desc
					pcall(function()
						desc = Players:GetHumanoidDescriptionFromUserId(p.UserId)
					end)
					if desc then
						table.insert(appearances, desc)
					end
				end
			end
		end
		while #appearances < count do
			table.insert(appearances, Instance.new("HumanoidDescription"))
		end
		return appearances
	end
	local function buildMinionModel(offsetCFrame, humanoidDesc)
		if not figureRef then return nil end
		local dummy
		if humanoidDesc then
			pcall(function()
				dummy = Players:CreateHumanoidModelFromDescription(
					humanoidDesc,
					Enum.HumanoidRigType.R6
				)
			end)
		end
		if not dummy then
			pcall(function()
				figureRef.Archivable = true
				dummy = figureRef:Clone()
				figureRef.Archivable = false
			end)
		end
		if not dummy then return nil end
		dummy.Name = "Ph0nk"
		dummy.Parent = workspace
		for _, v in ipairs(dummy:GetDescendants()) do
			if v:IsA("Script") or v:IsA("LocalScript") or v:IsA("Sound")
			or v:IsA("ScreenGui") or v:IsA("BillboardGui") or v:IsA("SurfaceGui") then
				v:Destroy()
			elseif v:IsA("BasePart") then
				v.Massless = true
				v.CanCollide = (v.Name == "HumanoidRootPart")
				v.CanTouch = true
				v.CanQuery = true
				v.Anchored = false
			end
		end
		local myRoot = figureRef:FindFirstChild("HumanoidRootPart") or figureRef:FindFirstChild("Torso")
		local dRoot = dummy:FindFirstChild("HumanoidRootPart") or dummy:FindFirstChild("Torso")
		if myRoot and dRoot then
			if dummy:IsA("Model") then
				dummy.PrimaryPart = dRoot
				dummy:PivotTo(myRoot.CFrame * offsetCFrame)
			else
				dRoot.CFrame = myRoot.CFrame * offsetCFrame
			end
		end
		if dRoot then dRoot.Anchored = true end
		return dummy
	end
	local function getClosestPlayer(minionRoot)
		local bestDist = math.huge
		local bestTarget = nil
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= Player and p.Character then
				local pRoot = p.Character:FindFirstChild("HumanoidRootPart")
				if pRoot then
					local dist = (pRoot.Position - minionRoot.Position).Magnitude
					if dist < bestDist then
						bestDist = dist
						bestTarget = p
					end
				end
			end
		end
		return bestTarget, bestDist
	end
	local function minionSetAnim(minionData, animKey, looped)
		if minionData.currentAnimKey == animKey then return end
		minionData.currentAnimKey = animKey
		minionData.animator.track = minionData.tracks[animKey]
		minionData.animator.looped = looped
		minionData.animTime = 0
	end
	local function moveMinionToward(minionRoot, targetPos, speed, dt)
		local dir = (targetPos - minionRoot.Position)
		dir = Vector3.new(dir.X, 0, dir.Z)
		local mag = dir.Magnitude
		if mag < 0.5 then return true end
		local step = dir.Unit * speed * dt
		if step.Magnitude > mag then step = dir end
		minionRoot.CFrame = CFrame.new(minionRoot.Position + step, minionRoot.Position + dir.Unit)
		return false
	end
	local function runPhonkMinionAI(md, dt)
		local root = md.model:FindFirstChild("HumanoidRootPart")
		if not root or not md.model.Parent then return false end
		local now = os.clock()
		md.animTime = md.animTime + dt
		md.animator:Step(md.animTime)
		if md.isFading then
			md.fadeElapsed = md.fadeElapsed + dt
			local alpha = math.clamp(1 - (md.fadeElapsed / 1.0), 0, 1)
			for _, part in ipairs(md.model:GetDescendants()) do
				if part:IsA("BasePart") then
					part.Transparency = 1 - alpha
				elseif part:IsA("Decal") or part:IsA("Texture") then
					part.Transparency = 1 - alpha
				end
			end
			if md.fadeElapsed >= 1.0 then
				md.model:Destroy()
				return false
			end
			return true
		end
		if md.isAttacking then
			local elapsed = now - md.attackStart
			if elapsed >= 0.5 then
				md.isAttacking = false
				md.isFading = true
				md.fadeElapsed = 0
			end
			return true
		end
		local target, dist = getClosestPlayer(root)
		if target and target.Character then
			local pRoot = target.Character:FindFirstChild("HumanoidRootPart")
			if pRoot then
				if dist <= 4 then
					minionSetAnim(md, "attack", false)
					md.isAttacking = true
					md.attackStart = now
					ReanimateFling(target.Character)
				else
					minionSetAnim(md, "walk", true)
					moveMinionToward(root, pRoot.Position, 8, dt)
				end
			end
		else
			minionSetAnim(md, "idle", true)
		end
		return true
	end
	local function spawnPhonkMinion(side, humanoidDesc)
		if not figureRef then return end
		local offset = (side == "left") and CFrame.new(-4, 0, 0) or CFrame.new(4, 0, 0)
		local animFile = (side == "left") and "PhonkMinionSpawnLeft.anim" or "PhonkMinionSpawnRight.anim"
		local model = buildMinionModel(offset, humanoidDesc)
		if not model then return end
		local spawnAnim = loadAnim(model, animFile, false)
		spawnAnim.weight = 1
		local minionTracks = {
			idle = AnimLib.Track.fromfile(AssetGetPathFromFilename("PhonkMinionIdle.anim")),
			walk = AnimLib.Track.fromfile(AssetGetPathFromFilename("PhonkMinionWalk.anim")),
			attack = AnimLib.Track.fromfile(AssetGetPathFromFilename("PhonkMinionAttack.anim")),
		}
		local minionAnim = AnimLib.Animator.new()
		minionAnim.rig = model
		minionAnim.track = minionTracks.idle
		minionAnim.looped = true
		minionAnim.speed = 1
		minionAnim.weight = 1
		local minionData = {
			model = model,
			animator = minionAnim,
			tracks = minionTracks,
			currentAnimKey = "idle",
			animTime = 0,
			spawnAnim = spawnAnim,
			spawnStart = os.clock(),
			spawnDone = false,
			isAttacking = false,
			attackStart = 0,
			isFading = false,
			fadeElapsed = 0,
		}
		table.insert(activeMinions, minionData)
		Debris:AddItem(model, 300)
	end
	local function destroyAllMinions()
		for _, md in ipairs(activeMinions) do
			if md.model and md.model.Parent then
				md.model:Destroy()
			end
		end
		activeMinions = {}
	end
	m.Init = function(figure)
		figureRef = figure
		local hum = figure:FindFirstChildOfClass("Humanoid")
		local root = figure:FindFirstChild("HumanoidRootPart")
		if not hum or not root then return end
		animator = AnimLib.Animator.new()
		animator.rig = figure
		animator.looped = true
		tracks.idle          = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKIDLE.anim"))
		tracks.walk          = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKWALK.anim"))
		tracks.run           = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKRUN.anim"))
		tracks.m1            = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKM1.anim"))
		tracks.corrupt       = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKCORRUPT.anim"))
		tracks.kill          = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKKILL.anim"))
		tracks.intro         = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKINTRO.anim"))
		tracks["7n7"]        = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONK7N7KILL.anim"))
		tracks.stunnedStart  = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKSTUNSTART.anim"))
		tracks.stunnedLoop   = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKSTUNLOOP.anim"))
		tracks.stunnedEnd    = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKSTUNEND.anim"))
		tracks.delivery      = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKDELIVERY.anim"))
		tracks.walkspeedStart = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKWALKSPEEDSTART.anim"))
		tracks.walkspeedLoop  = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKWALKSPEEDLOOP.anim"))
		tracks.walkspeedHit   = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKWALKSPEEDHIT.anim"))
		tracks.walkspeedFail  = AnimLib.Track.fromfile(AssetGetPathFromFilename("PHONKWALKSPEEDFAIL.anim"))
		currentTrack = tracks.idle
		animator.track = currentTrack
		SetOverrideMovesetMusic(AssetGetContentId("Phonk.mp3"), "READY FOR RUMBLE", 1)
		setupFlingTouched(figure)
		task.spawn(function()
			cachedMinionAppearances = prefetchMinionAppearances(2)
			appearancesCached = true
		end)
		ContextActions:BindAction("phonk_m1", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			flingActive = true
			playSound("PHONKM1.mp3", root)
			currentTrack = tracks.m1
			animator.track = currentTrack
			animationTime = 0
			task.wait(0.37)
			flingActive = false
			usingSkill = false
			lastState = "none"
		end, true, Enum.UserInputType.MouseButton1)
		ContextActions:BindAction("phonk_corrupt", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			flingActive = true
         hum.WalkSpeed = 0
			hum.JumpPower = 0
			playSound("PHONKCORRUPT.mp3", root)
			currentTrack = tracks.corrupt
			animator.track = currentTrack
			animationTime = 0
			task.wait(0.77)
			usingSkill = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState = "none"
		end, true, Enum.KeyCode.E)
		ContextActions:BindAction("phonk_kill", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			playSound("PHONKKILL.mp3", root)
			currentTrack = tracks.kill
			animator.track = currentTrack
			animationTime = 0
			spawnVictimNPC(
				CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(0), 0),
				"PHONKVICTIM.anim",
				2.42
			)
			task.wait(2.42)
			usingSkill = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState = "none"
		end, true, Enum.KeyCode.Z)
		ContextActions:BindAction("phonk_intro", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			playSound("PHONKINTRO.mp3", root)
			currentTrack = tracks.intro
			animator.track = currentTrack
			animationTime = 0
			task.wait(3.33)
			usingSkill = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState = "none"
		end, true, Enum.KeyCode.One)
		ContextActions:BindAction("phonk_7n7_kill", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			playSound("PHONK7N7KILL.mp3", root)
			playSound("PHONK7N7KILLEFFECT.mp3", root)
			currentTrack = tracks["7n7"]
			animator.track = currentTrack
			animationTime = 0
			spawnVictimNPC(
				CFrame.new(0, 0, 0) * CFrame.Angles(0, math.rad(0), 0),
				"PHONK7N7VICTIM.anim",
				4.03
			)
			task.wait(4.03)
			usingSkill = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState = "none"
		end, true, Enum.KeyCode.Two)
		ContextActions:BindAction("phonk_stun", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			playSound("PHONKSTUN.mp3", root)
			currentTrack = tracks.stunnedStart
			animator.track = currentTrack
			animationTime = 0
			task.wait(0.58)
			currentTrack = tracks.stunnedLoop
			animator.track = currentTrack
			animationTime = 0
			task.wait(0.58)
			currentTrack = tracks.stunnedEnd
			animator.track = currentTrack
			animationTime = 0
			task.wait(1.03)
			usingSkill = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState = "none"
		end, true, Enum.KeyCode.Three)
		ContextActions:BindAction("phonk_delivery", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			playSound("PHONKDELIVERY.mp3", root)
			playSound("PHONKDELIVERYVOICE.mp3", root)
			currentTrack = tracks.delivery
			animator.track = currentTrack
			animationTime = 0
			destroyAllMinions()
			local minionAppearances
			if appearancesCached and #cachedMinionAppearances >= 2 then
				minionAppearances = cachedMinionAppearances
				task.spawn(function()
					appearancesCached = false
					cachedMinionAppearances = prefetchMinionAppearances(2)
					appearancesCached = true
				end)
			else
				minionAppearances = prefetchMinionAppearances(2)
				cachedMinionAppearances = minionAppearances
				appearancesCached = true
			end
			task.delay(0.90, function()
				if usingSkill then spawnPhonkMinion("left", minionAppearances[1]) end
			end)
			task.delay(1.73, function()
				if usingSkill then spawnPhonkMinion("right", minionAppearances[2]) end
			end)
			task.wait(2.90)
			usingSkill = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState = "none"
		end, true, Enum.KeyCode.R)
		ContextActions:BindAction("phonk_walk", function(_, state)
			if state ~= Enum.UserInputState.Begin or usingSkill then return end
			usingSkill = true
			voidHit = false
			voidActive = false
			hum.WalkSpeed = 0
			hum.JumpPower = 0
			playSound("PHONKWALKSPEEDSTART.mp3", root)
			currentTrack = tracks.walkspeedStart
			animator.track = currentTrack
			animationTime = 0
			task.wait(0.75)
			local loopSnd = Instance.new("Sound")
			loopSnd.SoundId = AssetGetContentId("PHONKWALKSPEEDLOOP.mp3")
			loopSnd.Volume = 1.5
			loopSnd.Looped = true
			loopSnd.Parent = root
			loopSnd:Play()
			currentTrack = tracks.walkspeedLoop
			animator.track = currentTrack
			animationTime = 0
			voidActive = true
			local hrp = figure:FindFirstChild("HumanoidRootPart")
			local startTime = os.clock()
			local lockedDir = hrp and hrp.CFrame.LookVector or Vector3.new(0, 0, -1)
			hum.WalkSpeed = 0
			hum.AutoRotate = false
			local bv = Instance.new("BodyVelocity")
			bv.Velocity = lockedDir * 90
			bv.MaxForce = Vector3.new(1e5, 0, 1e5)
			bv.P = 1e5
			bv.Parent = hrp
			while (os.clock() - startTime < 1.34) and not voidHit and usingSkill do
				task.wait()
			end
			bv:Destroy()
			hum.AutoRotate = true
			hum.WalkSpeed = 0
			voidActive = false
			if loopSnd then
				loopSnd:Stop()
				loopSnd:Destroy()
			end
			if voidHit and usingSkill then
				playSound("PHONKWALKSPEEDHIT.mp3", root)
				currentTrack = tracks.walkspeedHit
				animator.track = currentTrack
				animationTime = 0
				flingActive = true
				task.wait(1.23)
				flingActive = false
			else
				playSound("PHONKWALKSPEEDFAIL.mp3", root)
				currentTrack = tracks.walkspeedFail
				animator.track = currentTrack
				animationTime = 0
				task.wait(0.70)
			end
			usingSkill = false
			hum.WalkSpeed = 8
			hum.JumpPower = 50
			lastState = "none"
		end, true, Enum.KeyCode.Q)
		ContextActions:BindAction("phonk_sprint", function(_, state)
			if state == Enum.UserInputState.Begin then
				isSprinting = not isSprinting
				if isSprinting then
					ContextActions:SetTitle("phonk_sprint", "Walk")
				else
					ContextActions:SetTitle("phonk_sprint", "Sprint")
				end
			end
		end, true, Enum.KeyCode.LeftShift)
		ContextActions:SetTitle("phonk_m1", "M1")
		ContextActions:SetTitle("phonk_corrupt", "Corrupt")
		ContextActions:SetTitle("phonk_kill", "Kill")
		ContextActions:SetTitle("phonk_walk", "Walkspeed")
		ContextActions:SetTitle("phonk_sprint", "Sprint")
		ContextActions:SetTitle("phonk_intro", "Intro")
		ContextActions:SetTitle("phonk_7n7_kill", "7n7Kill")
		ContextActions:SetTitle("phonk_stun", "Stun")
		ContextActions:SetTitle("phonk_delivery", "Delivery")
		ContextActions:SetPosition("phonk_m1", UDim2.new(1, -70, 1, -70))
		ContextActions:SetPosition("phonk_corrupt", UDim2.new(1, -140, 1, -70))
		ContextActions:SetPosition("phonk_kill", UDim2.new(1, -210, 1, -70))
		ContextActions:SetPosition("phonk_walk", UDim2.new(1, -280, 1, -70))
		ContextActions:SetPosition("phonk_sprint", UDim2.new(1, -350, 1, -70))
		ContextActions:SetPosition("phonk_intro", UDim2.new(1, -70, 1, -150))
		ContextActions:SetPosition("phonk_7n7_kill", UDim2.new(1, -140, 1, -150))
		ContextActions:SetPosition("phonk_stun", UDim2.new(1, -210, 1, -150))
		ContextActions:SetPosition("phonk_delivery", UDim2.new(1, -280, 1, -150))
	end
	m.Update = function(dt, figure)
		if not animator then return end
		local hum = figure:FindFirstChildOfClass("Humanoid")
		local root = figure:FindFirstChild("HumanoidRootPart")
		if not hum then return end
		animationTime += dt
		animator:Step(animationTime)
		local now = os.clock()
		for i = #activeVictims, 1, -1 do
			local data = activeVictims[i]
			if data.dummy and data.dummy.Parent then
				data.anim:Step(now - data.start)
			else
				table.remove(activeVictims, i)
			end
		end
		for i = #activeMinions, 1, -1 do
			local md = activeMinions[i]
			if not md.model or not md.model.Parent then
				table.remove(activeMinions, i)
			else
				if not md.spawnDone then
					local elapsed = now - md.spawnStart
					md.spawnAnim:Step(elapsed)
					if elapsed >= 1.0 then
						md.spawnDone = true
					end
				else
					runPhonkMinionAI(md, dt)
				end
			end
		end
		if usingSkill then return end
		local state = "idle"
		if hum.MoveDirection.Magnitude > 0.1 then
			if isSprinting then
				state = "run"
			else
				state = "walk"
			end
		end
		if lastState ~= state then
			animationTime = 0
			lastState = state
		end
		local newTrack
		if state == "idle" then
			newTrack = tracks.idle
			hum.WalkSpeed = 8
		elseif state == "walk" then
			newTrack = tracks.walk
			hum.WalkSpeed = 8
		elseif state == "run" then
			newTrack = tracks.run
			hum.WalkSpeed = 27.5
		end
		if currentTrack ~= newTrack then
			currentTrack = newTrack
			animator.track = currentTrack
		end
		animator:Step(animationTime)
	end
	m.Destroy = function()
		clearObserve()
		destroyAllMinions()
		voidActive = false
		voidHit = false
		isSprinting = false
		for _, conn in ipairs(touchConns) do
			conn:Disconnect()
		end
		table.clear(touchConns)
		for _, data in ipairs(activeVictims) do
			if data.dummy then
				data.dummy:Destroy()
			end
		end
		activeVictims = {}
		ContextActions:UnbindAction("phonk_m1")
		ContextActions:UnbindAction("phonk_corrupt")
		ContextActions:UnbindAction("phonk_kill")
		ContextActions:UnbindAction("phonk_intro")
		ContextActions:UnbindAction("phonk_7n7_kill")
		ContextActions:UnbindAction("phonk_stun")
		ContextActions:UnbindAction("phonk_delivery")
		ContextActions:UnbindAction("phonk_walk")
		ContextActions:UnbindAction("phonk_sprint")
		animator = nil
		tracks = {}
		currentTrack = nil
		figureRef = nil
		flingActive = false
		usingSkill = false
	end
	return m
end)
return modules
end)
if not ok then
    (warn or print)("[KernelPacker] failed to load UPDATED mind of him.lua: " .. tostring(result))
elseif type(result) == "table" and result ~= modules then
    for _, fn in ipairs(result) do table.insert(modules, fn) end
end
end

return modules
